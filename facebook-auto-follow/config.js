// Behavior config — delays are in ms and get randomized in-range
export const config = {
  scrollDelayMin: 1500,
  scrollDelayMax: 4000,
  actionDelayMin: 400,
  actionDelayMax: 1200,
  hoverDelayMin: 300,
  hoverDelayMax: 600,
  readDelayMin: 2000,
  readDelayMax: 5000,
  maxRetries: 2,
  debugMode: false
}
