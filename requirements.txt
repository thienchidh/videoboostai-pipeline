# VideoBoostAI Pipeline Dependencies
# Auto-install with: pip install -r requirements.txt

# Core
requests>=2.25.0
PyYAML>=6.0
pydantic>=2.0

# Database
psycopg2-binary>=2.9.0
pgvector>=0.2.0
numpy>=1.24.0

# Embeddings (semantic dedup)
sentence-transformers>=2.2.0

# Image processing
Pillow>=9.0.0

# TTS - edge-tts CLI (pip install edge-tts)
# Run: pip install edge-tts
edge-tts>=7.0.0

# Whisper timestamps - OpenAI Whisper Python (pip install openai-whisper)
# Run: pip install openai-whisper
# Note: openai-whisper installs torch (~2GB). For CI/testing, mock in tests/
openai-whisper>=20231117

# Scheduler (for cron_manager.py)
schedule>=1.1.0

# S3 Storage (MinIO) for media uploads
boto3>=1.26.0

# Retry / backoff
tenacity>=8.2.0

# Testing
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0
